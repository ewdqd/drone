# 动作服务 Dockerfile
FROM openjdk:17-jdk-slim

WORKDIR /app

COPY drone-action-service/target/drone-action-service-*.jar app.jar

EXPOSE 9094 20880

ENV JAVA_OPTS="-Xms256m -Xmx512m"

ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
