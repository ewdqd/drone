server:
  port: 9094

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dec?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: 2410120044
    driver-class-name: com.mysql.cj.jdbc.Driver

dubbo:
  application:
    name: drone-action-service
  registry:
    address: zookeeper://localhost:2181
  protocol:
    name: dubbo
    port: -1
  provider:
    timeout: 5000
