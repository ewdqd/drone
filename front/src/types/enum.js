export const ERouterName = Object.freeze({
    MEMBERS: 'members',
    HOME:   '/admin/home',
    Login:   '/admin/login'
    // ... 其他属性
})
export const  ELocalStorageKey=Object.freeze({
    Username: 'username',
    Token:'x-auth-token',
    UserId: 'user_id',
})

export const  EUserType=Object.freeze({
    guest: 1,
    admin: 2
})
